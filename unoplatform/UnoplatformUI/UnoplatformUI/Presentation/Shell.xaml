<UserControl x:Class="UnoplatformUI.Presentation.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:UnoplatformUI.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d">
  <Grid Background="{ThemeResource BackgroundBrush}"
        utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Frame x:Name="ContentFrame" Grid.Row="0" />

    <utu:TabBar x:Name="MainTabBar" Grid.Row="1" SelectionChanged="OnTabSelectionChanged">
      <utu:TabBarItem Content="New" />
      <utu:TabBarItem Content="All" />
      <utu:TabBarItem Content="Paged" />
      <utu:TabBarItem Content="Settings" />
    </utu:TabBar>
  </Grid>
</UserControl>
